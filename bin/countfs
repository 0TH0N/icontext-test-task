#!/usr/bin/env php

<?php

require_once __DIR__ . '/../src/autoload.php';

$fieldsCount = $argv[1];
$chipCount = $argv[2];
$filePath = $argv[3] ?? 'result.txt';

IContextTest\Index\run($fieldsCount, $chipCount, $filePath);
